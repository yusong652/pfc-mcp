{
  "name": "structure-interface",
  "dimension": "mixed",
  "full_name": "Structural and Interface FISH Intrinsics",
  "description": "Structural and interface FISH intrinsic families are used to extract forces, moments, link state, interface contact response, and reinforcement state after solving.",
  "families": [
    {
      "family": "structural nodes and elements",
      "examples": [
        "struct.node.list",
        "struct.node.pos",
        "struct.node.disp.global",
        "struct.link.list",
        "struct.link.model"
      ],
      "notes": [
        "Use 'struct.node.disp.global(node_pnt)' for nodal displacement; 'struct.node.disp' alone is NOT a valid intrinsic in FLAC2D 9.0.",
        "Structural node values are central for support displacement and link-state checks.",
        "Use element-specific families for cable, beam, pile, and liner quantities (shell/geogrid types do not exist in FLAC2D)."
      ]
    },
    {
      "family": "reinforcement response",
      "examples": [
        "struct.cable.force.axial",
        "struct.beam.moment",
        "struct.liner.normal.stress"
      ],
      "flac3d_only_intrinsics": [
        "struct.beam.force",
        "struct.shell.stress"
      ],
      "notes": [
        "FISH intrinsic naming differs between FLAC2D and FLAC3D: 'struct.beam.force' and 'struct.shell.stress' exist in FLAC3D 9.0 but are rejected in FLAC2D 9.0 (FLAC2D has 'struct.beam.moment' and no shell type).",
        "Plot 'structure-beam contour force' attribute names also do NOT mirror FISH intrinsic names directly.",
        "Probe live with 'fish list intrinsics' (paginate output) when an expected name is rejected.",
        "Choose local/global force interpretation deliberately (e.g. 'struct.node.disp.global' vs '.local')."
      ]
    },
    {
      "family": "zone interfaces",
      "examples": [
        "interface.list",
        "interface.node.list",
        "interface.node.stress.normal"
      ],
      "notes": [
        "FLAC2D 9.0 uses the 'interface.*' (or alias 'int.*') prefix — NOT 'zone.interface.*'.",
        "Interface data are often node based; inspect active contact state before interpreting forces.",
        "Interface stiffness and strength should be validated against adjacent zone stiffness and expected discontinuity behavior."
      ]
    }
  ],
  "common_checks": [
    "Maximum cable axial load compared with yield capacity.",
    "Liner or shell force envelopes after staged excavation.",
    "Interface slip/opening indicators along discontinuities.",
    "Link attachment state after geometry updates or large-strain motion."
  ],
  "official_documentation": "https://docs.itascacg.com/itasca900/flac3d/docproject/source/flac3dhome.html"
}
