{
  "name": "zone-gridpoint",
  "dimension": "mixed",
  "full_name": "Zone and Gridpoint FISH Intrinsics",
  "description": "Zone and gridpoint FISH intrinsics provide pointer-based access to model objects. They are best used for scripted checks, custom measurements, and compact validation reports after command-driven setup.",
  "intrinsic_families": [
    {
      "family": "object iteration",
      "examples": [
        "zone.list",
        "gp.list",
        "zone.num",
        "gp.num"
      ],
      "notes": [
        "Use loop foreach over pointer lists instead of assuming object IDs are contiguous.",
        "Prefer named groups and ranges for stable object selection."
      ]
    },
    {
      "family": "position and topology",
      "examples": [
        "zone.pos",
        "gp.pos",
        "zone.gp",
        "zone.face.normal",
        "zone.face.gp",
        "zone.id",
        "gp.id"
      ],
      "notes": [
        "FLAC2D positions use x/y model space; FLAC3D positions use x/y/z.",
        "Use 'zone.face.normal(z, side)' for face normal vectors; 'zone.face.gp(z, side, i)' for face gridpoints. 'zone.face' alone is NOT a valid intrinsic in FLAC2D 9.0.",
        "Zone face and node orientation differs between 2D and 3D zone shapes."
      ]
    },
    {
      "family": "state and properties",
      "examples": [
        "zone.model",
        "zone.prop",
        "zone.state",
        "zone.group",
        "zone.extra",
        "gp.group",
        "gp.extra"
      ],
      "notes": [
        "Use 'zone.model(z)' to query the constitutive-model name; 'zone.cmodel' is NOT a valid intrinsic in FLAC2D 9.0.",
        "Use property keywords from the constitutive-models reference with zone.prop.",
        "Use groups rather than geometric coordinates when checking staged regions."
      ]
    },
    {
      "family": "mechanical response",
      "examples": [
        "zone.stress",
        "zone.strain.inc",
        "gp.disp",
        "gp.vel",
        "gp.force.unbal"
      ],
      "notes": [
        "Vector/tensor component availability follows FLAC2D/FLAC3D product rules.",
        "Use histories for long runs and FISH intrinsics for snapshots or derived quantities."
      ]
    }
  ],
  "patterns": [
    {
      "name": "Maximum unbalanced force scan",
      "fish": [
        "fish define max_unbal",
        "  local m = 0.0",
        "  loop foreach local g gp.list",
        "    m = math.max(m, math.mag(gp.force.unbal(g)))",
        "  end_loop",
        "  max_unbal = m",
        "end"
      ]
    },
    {
      "name": "Group-based zone count",
      "fish": [
        "fish define count_named_zones",
        "  local n = 0",
        "  loop foreach local z zone.list",
        "    if zone.group(z) = 'excavation'",
        "      n += 1",
        "    endif",
        "  end_loop",
        "  count_named_zones = n",
        "end"
      ]
    }
  ],
  "official_documentation": "https://docs.itascacg.com/itasca900/flac3d/docproject/source/elements/zones/zones.html"
}
