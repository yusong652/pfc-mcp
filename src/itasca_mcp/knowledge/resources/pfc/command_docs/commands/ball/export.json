{
  "category": "ball",
  "search_keywords": [
    "export",
    "geometry",
    "STL"
  ],
  "description": "Export balls to geometry sets for post-processing or STL export. If no range is specified, all balls in the model are exported. Currently only supports export to geometry sets.",
  "geometry_keywords": [
    {
      "name": "set",
      "syntax": "set <s>",
      "description": "Geometry set base name (default: 'balls'). With 'split', creates sets named '<s>_ball<id>'"
    },
    {
      "name": "resolution",
      "syntax": "resolution <f>",
      "description": "Facet edge resolution factor. Edge length < f * R where R is ball radius (default: 0.25)"
    },
    {
      "name": "split",
      "syntax": "split",
      "description": "Export each ball to dedicated geometry set (default: all balls in one set)"
    },
    {
      "name": "positions",
      "syntax": "positions",
      "description": "Create only geometry nodes from ball positions (no triangulated surfaces)"
    }
  ],
  "python_sdk_alternative": {
    "available": false,
    "workaround": "itasca.command('ball export geometry ...') - no Python SDK equivalent for geometry export"
  },
  "versions": {
    "7.0": {
      "command": "ball export",
      "syntax": "ball export keyword [range]",
      "keywords": [
        {
          "name": "split",
          "syntax": "split",
          "description": "Specify that only geometry nodes are to be created from the ball positions. Resolution of the facet edges. The number of facets is set so that the edge length is smaller than f * R , where R is the ball radius. By default, f = 0.25. Set the geometry set base name to s . If the split keyword is specified, then each ball is exported into a dedicated geometry set with a name corresponding to the base name to which the word ball and the ball ID is appended (e.g. ‘myset_ball1’ for the ball with ID 1). Note that in this case, existing sets with the same name are overridden. Otherwise, all balls are exported into one unique geometry set with name s , and existing geometry objects in this set are not deleted. The default geometry set base name is balls . If specified, the split keyword results in each ball being exported into a dedicated geometry set. By default, all the balls are exported into one unique geometry set (see above)."
        }
      ],
      "examples": [
        {
          "command": "ball export geometry",
          "description": "Export all balls as triangulated geometry to set 'balls' with default resolution 0.25"
        },
        {
          "command": "ball export geometry set 'my_balls' resolution 0.1",
          "description": "Export balls with higher resolution (0.1) to geometry set 'my_balls'"
        },
        {
          "command": "ball export geometry split set 'ball_group'",
          "description": "Export each ball to separate geometry set (e.g., 'ball_group_ball1', 'ball_group_ball2')"
        },
        {
          "command": "ball export geometry positions range group 'layer1'",
          "description": "Export only ball center positions as geometry nodes for balls in 'layer1' group"
        },
        {
          "command": "ball export geometry resolution 0.5 range id 1 100",
          "description": "Export balls 1-100 with lower resolution (coarser triangulation)"
        }
      ]
    },
    "6.0": {
      "command": "ball export",
      "syntax": "ball export keyword [range]",
      "keywords": [
        {
          "name": "split",
          "syntax": "split",
          "description": "Specify that only geometry nodes are to be created from the ball positions. Resolution of the facet edges. The number of facets is set so that the edge length is smaller than f * R , where R is the ball radius. By default, f = 0.25. Set the geometry set base name to s . If the split keyword is specified, then each ball is exported into a dedicated geometry set with a name corresponding to the base name to which the word ball and the ball ID is appended (e.g. ‘myset_ball1’ for the ball with ID 1). Note that in this case, existing sets with the same name are overridden. Otherwise, all balls are exported into one unique geometry set with name s , and existing geometry objects in this set are not deleted. The default geometry set base name is balls . If specified, the split keyword results in each ball being exported into a dedicated geometry set. By default, all the balls are exported into one unique geometry set (see above)."
        }
      ],
      "examples": [
        {
          "command": "ball export geometry",
          "description": "Export all balls as triangulated geometry to set 'balls' with default resolution 0.25"
        },
        {
          "command": "ball export geometry set 'my_balls' resolution 0.1",
          "description": "Export balls with higher resolution (0.1) to geometry set 'my_balls'"
        },
        {
          "command": "ball export geometry split set 'ball_group'",
          "description": "Export each ball to separate geometry set (e.g., 'ball_group_ball1', 'ball_group_ball2')"
        },
        {
          "command": "ball export geometry positions range group 'layer1'",
          "description": "Export only ball center positions as geometry nodes for balls in 'layer1' group"
        },
        {
          "command": "ball export geometry resolution 0.5 range id 1 100",
          "description": "Export balls 1-100 with lower resolution (coarser triangulation)"
        }
      ]
    },
    "9.0": {
      "command": "ball export",
      "syntax": "ball export keyword [range]",
      "keywords": [
        {
          "name": "split",
          "syntax": "split",
          "description": "Specify that only geometry nodes are to be created from the ball positions. Resolution of the facet edges. The number of facets is set so that the edge length is smaller than f * R , where R is the ball radius. By default, f = 0.25. Set the geometry set base name to s . If the split keyword is specified, then each ball is exported into a dedicated geometry set with a name corresponding to the base name to which the word ball and the ball ID is appended (e.g. ‘myset_ball1’ for the ball with ID 1). Note that in this case, existing sets with the same name are overridden. Otherwise, all balls are exported into one unique geometry set with name s , and existing geometry objects in this set are not deleted. The default geometry set base name is balls . If specified, the split keyword results in each ball being exported into a dedicated geometry set. By default, all the balls are exported into one unique geometry set (see above)."
        }
      ],
      "examples": [
        {
          "command": "ball export geometry",
          "description": "Export all balls as triangulated geometry to set 'balls' with default resolution 0.25"
        },
        {
          "command": "ball export geometry set 'my_balls' resolution 0.1",
          "description": "Export balls with higher resolution (0.1) to geometry set 'my_balls'"
        },
        {
          "command": "ball export geometry split set 'ball_group'",
          "description": "Export each ball to separate geometry set (e.g., 'ball_group_ball1', 'ball_group_ball2')"
        },
        {
          "command": "ball export geometry positions range group 'layer1'",
          "description": "Export only ball center positions as geometry nodes for balls in 'layer1' group"
        },
        {
          "command": "ball export geometry resolution 0.5 range id 1 100",
          "description": "Export balls 1-100 with lower resolution (coarser triangulation)"
        }
      ]
    }
  }
}
